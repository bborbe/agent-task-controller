apiVersion: v1
kind: Service
metadata:
  name: 'agent-task-controller-{{ "VAULT" | env }}'
  namespace: '{{ "NAMESPACE" | env }}'
  annotations:
    admin/port: '9090'
    admin/path: ''
spec:
  ports:
  - name: http
    port: 9090
  selector:
    app: 'agent-task-controller-{{ "VAULT" | env }}'
